name: End to end tests

on: push

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - run: chmod +x ./e2e.sh
    - run: ./e2e.sh build
    - run: ./e2e.sh up
    - run: ./e2e.sh run
    - run: ./e2e.sh stop
