name: End to end tests

on: push

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker-compose --file docker-compose.e2e.yml up --build --exit-code-from e2e
