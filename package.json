{
  "name": "alexandre-website",
  "version": "1.0.0",
  "description": "Alexandre Djerbetian - personal website",
  "scripts": {
    "bootstrap": "npm ci && lerna bootstrap --ci",
    "dev": "docker-compose --file docker-compose.dev.yml up --build",
    "test:e2e": "docker-compose up --build --exit-code-from e2e"
  },
  "dependencies": {
    "lerna": "^3.20.2"
  },
  "devDependencies": {
    "prettier": "^2.0.4"
  }
}
