{
    "name": "entities",
    "version": "0.0.0",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "rm -rf ./dist && npm run compile",
        "compile": "tsc --project tsconfig.build.json"
    },
    "dependencies": {
        "@types/node": "^13.13.2",
        "ts-node": "^8.9.0",
        "typescript": "^3.8.3"
    }
}
